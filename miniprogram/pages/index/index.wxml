<view class="container">
  <home-panel wx:if="{{choose_index==0}}" book_data="{{book_data}}">首页</home-panel>
  <my wx:if="{{choose_index==2}}">我的</my>
  <index-tabbar bindchange="onTabChange" tabbar_data="{{tabbar_data}}"></index-tabbar>
</view>